# Supabase project URL (safe to expose).
NEXT_PUBLIC_SUPABASE_URL=https://mmoxgmvrpiaflfnsrynx.supabase.co

# Supabase service_role secret. SERVER ONLY — never prefix with NEXT_PUBLIC_,
# never commit a real value, never import it from a client component.
SUPABASE_SERVICE_ROLE_KEY=

# GitHub repo to read release download counts from.
GITHUB_REPO=DibbayajyotiRoy/fresco

# Optional: a GitHub token raises the API rate limit (60/hr -> 5000/hr).
GITHUB_TOKEN=
