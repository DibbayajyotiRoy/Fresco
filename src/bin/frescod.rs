// Fresco wallpaper daemon — stub; real implementation lands in src/daemon/.
fn main() {
    env_logger::init();
    eprintln!("frescod: daemon not yet implemented (scaffold stub)");
    std::process::exit(1);
}
